import React, { useContext } from "react";
import {
  PDFDownloadLink,
  Document,
  Page,
  View,
  Text,
} from "@react-pdf/renderer";

const Invoice = () => (
  <Document>
    <Page>
      <View>
        <Text>hihihihi</Text>
      </View>
    </Page>
  </Document>
);

export default Invoice;
